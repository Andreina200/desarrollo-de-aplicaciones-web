- name: Checkout
  uses: actions/checkout@v4

- name: Setup Pages
  uses: actions/configure-pages@v5

- name: Upload artifact
  uses: actions/upload-pages-artifact@v3
  with:
    path: Semana 4/mi-sitio-bootstrap

- name: Deploy to GitHub Pages
  id: deployment
        uses: actions/deploy-pages@v4
